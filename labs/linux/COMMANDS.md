# Linus commands I might want reference later

$ service [UNIT] status
$ service --status-all
$ sudo apt-get install tasksel 
$ sudo tasksel install ...
$ sudo ufw app list
$ sudo ufw allow in "[UNIT]"